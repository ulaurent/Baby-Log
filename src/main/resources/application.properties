# Web Server Configuration
server.port=${PORT:10000}

# Database Configuration
# 1. Use the full hostname from your Render dashboard (Internal URL preferred)
# 2. Ensure "jdbc:postgresql://" is the prefix
# 3. Include the database name at the end of the URL
spring.datasource.url=
spring.datasource.password=
spring.datasource.driver-class-name=org.postgresql.Driver

# Connection Settings
# Adding SSL is often required for remote connections to Render
